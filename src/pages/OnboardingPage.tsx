
import { useState } from 'react'
import { CheckCircle2,ChevronRight,Building2,FolderKanban,Briefcase,Upload,Users } from 'lucide-react'
import { useNavigate } from 'react-router-dom'
const steps:Array<[string,string,any]>=[['Welcome','Understand SiteFlow PM and your workspace.',Building2],['Portfolio','Create or confirm your delivery portfolio.',FolderKanban],['Project','Create your first project.',Briefcase],['Schedule','Upload the baseline schedule.',Upload],['Team','Invite team members and assign access.',Users]]
export default function OnboardingPage(){const nav=useNavigate();const [step,setStep]=useState(0);const [done,setDone]=useState<number[]>([]);const current=steps[step];return <div className="-m-4 min-h-screen bg-[#f6f5f1] p-6"><div className="mx-auto max-w-5xl"><section className="rounded-[28px] border bg-white p-8"><div className="text-xs uppercase tracking-[.18em] text-[#df5f41]">Getting started</div><h1 className="mt-2 text-4xl font-semibold text-[#102943]">Welcome to SiteFlow PM</h1><p className="mt-3 text-[#6f7d89]">Complete these five steps to prepare your workspace for live project delivery.</p><div className="mt-8 grid gap-3 md:grid-cols-5">{steps.map((s,i)=>{const Icon=s[2] as any;return <button key={i} onClick={()=>setStep(i)} className={`rounded-2xl border p-4 text-left ${step===i?'border-[#173f5f] bg-[#eef5f8]':''}`}><Icon size={18}/><div className="mt-3 text-sm font-semibold">{s[0]}</div>{done.includes(i)&&<CheckCircle2 className="mt-2 text-emerald-600" size={16}/>}</button>})}</div><div className="mt-8 rounded-2xl bg-[#173f5f] p-8 text-white"><h2 className="text-2xl font-semibold">{current[0]}</h2><p className="mt-3 text-white/70">{current[1]}</p><div className="mt-6 flex flex-wrap gap-2"><button onClick={()=>{setDone(Array.from(new Set([...done,step])));if(step<steps.length-1)setStep(step+1)}} className="rounded-xl bg-white px-5 py-3 font-semibold text-[#173f5f]">Mark complete <ChevronRight className="inline" size={16}/></button>{step===2&&<button onClick={()=>nav('/app/projects')} className="rounded-xl bg-white/10 px-5 py-3">Open Projects</button>}{step===3&&<button onClick={()=>nav('/app/schedule')} className="rounded-xl bg-white/10 px-5 py-3">Open Schedule</button>}{step===4&&<button onClick={()=>nav('/app/team-access')} className="rounded-xl bg-white/10 px-5 py-3">Invite Team</button>}</div></div><div className="mt-6 flex justify-between text-sm text-[#87929b]"><span>{done.length} of {steps.length} completed</span><button onClick={()=>nav('/app')}>Go to dashboard</button></div></section></div></div>}
